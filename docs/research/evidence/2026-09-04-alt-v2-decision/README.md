# September 4 Alt V2 paired decision evidence

Source: SELECT-only reads of Supabase project `htoaytcsjrdyyzcwxjfg` on
September 4, 2026, 21:58–22:01Z. Exact SQL and capture timestamps are inside
each source JSON. The bounded date range is July 24–September 3, inclusive.
No runtime job, provider request, history mutation or notification was issued.

| Captured source | Contents and purpose |
| --- | --- |
| [selected-lock-proofs.json](selected-lock-proofs.json) | 113 full frozen V2 selected rows and exact consumed operational locks; immutable hypothesis, source metadata, decision proof |
| [bounded-history.json](bounded-history.json) | 978 bounded rows from the current published history, with artifact hash/publication metadata; 726 closed locked outcomes |
| [comparison-universe.json](comparison-universe.json) | 727 operational locks and 720 frozen V2 states, including pending and not-selected; normalized frozen inputs for comparison slices |
| [archive-context.json](archive-context.json) | 42 dated artifacts with 1,078 pitcher-context rows; tracked-pick quote/time association permits 725/726 lock-consistent contextual joins, not proof of frozen features |
| [bounded-close-snapshots.json](bounded-close-snapshots.json) | 458 snapshots for 113 exact selected targets, preflight index-plan evidence and 101-row per-target cap; no capped target |

The history payload hash identifies the full hosted artifact, while the file
hashes in [analysis.json](analysis.json) identify the exact bounded exports.
These are different hashes of different objects. JSON exports retain only
the SQL-selected fields; do not hash the subset as if it were the full hosted
artifact. The source files are intentional evidence, not scratch output.

Run from the repo root:

```bash
.venv/bin/python docs/research/evidence/2026-09-04-alt-v2-decision/reproduce.py
```

Python 3.11+ is required. The script reuses existing pure proof-validation,
close-producer and CLV-classification code. It has no database/network path;
it writes only `analysis.json`, `row-level-analysis.json`,
`official-close-packet.json` and `required-slices.md` in this directory.
Preserve the captured source inputs. A later live refresh should use a new
dated packet rather than overwrite this decision's evidence.

[decision-checks.ipynb](decision-checks.ipynb) is an executed companion with
the scoring checks, complete denominator accounting, overlap comparison and
CLV exclusions. Run its cells from this directory with a Python 3.11+ kernel.
The checked-in outputs were generated by real offline kernel execution.

The existing suites `tests/test_alternative_pick_evaluation_proof_v2.py` and
`tests/test_clv_official_close_packet.py` passed 105 tests. The project venv
lacked pytest; test/notebook dependencies were installed only into a temporary
directory outside the repository. No requirements or production environment
changed.

Known exceptions remain explicit: one unselected Kumar Rocker LEAN lacks
canonical history; one selected void lacks lock-consistent Path B context;
23 selected targets lack an eligible exact close. Neither missing outcomes
nor missing closes are filled with zeros or inferred observations.
